// 삼항연산자 (중요!)
// 조건식 ? 참 : 거짓
// 조건에 따라 값을 선택한다.

let x = 30;
let result = (x > 5) ? "크다" : "작다";
console.log(result); // 크다 : 30 > 5

let answer = false;
let result2 = answer ? 1 : 2;
console.log(result2); // 2